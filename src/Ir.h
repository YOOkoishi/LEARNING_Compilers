#pragma once

#include "include.h"

enum class IRValue {
    Interger,
    ZeroInit,
    FuncArgRef,
    GlobalAlloc,
    Alloc,
    Load,
    Store,
    GetPtr,
    GetElemPtr,
    Binary,
    Branch,
    Jump,
    Call,
    Return
};

class IRBasicBlock {

};

class IRFunction {

};

class IRProgram {

};