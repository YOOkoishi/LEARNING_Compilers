// 测试用例7: 三维数组 - 部分省略
// int arr[2][2][3] = {{{1}, {2}}, {{3}}}
// 预期内存布局: [1,0,0, 2,0,0, 3,0,0, 0,0,0]

int main() {
    int arr[2][2][3] = {{{1}, {2}}, {{3}}};
    
    // 验证每个元素
    int i = 0;
    while (i < 12) {
        putint(arr[i]);
        putch(32);
        i = i + 1;
    }
    putch(10);
    
    return 0;
}
